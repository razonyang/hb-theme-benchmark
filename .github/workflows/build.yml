name: Build

on:
  push:

jobs:
  build:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        posts: [1000, 2000, 3000, 4000, 5000, 6000]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - run: npm ci

      - uses: actions/setup-go@v5

      - uses: actions/cache@v4
        with:
          path: resources
          key: hugo-resources

      - uses: actions/cache@v4
        with:
          path: /home/runner/.cache/hugo_cache
          key: ${{ runner.os }}-hugomod-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-hugomod-

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          extended: true

      - run: ./node_modules/.bin/lorem-ipsum-generator -n ${{ matrix.posts }}

      - name: Build
        run: hugo --minify | npx hugo-metrics-parser
